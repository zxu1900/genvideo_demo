# 📧 邮件发送配置指南

## 🔧 设置步骤

### 1. 创建 Gmail App Password

1. **登录 Google 账户**
2. **进入安全设置**: https://myaccount.google.com/security
3. **启用两步验证** (如果还没启用)
4. **生成应用专用密码**:
   - 点击 "应用专用密码"
   - 选择 "邮件"
   - 生成 16 位密码

### 2. 配置环境变量

在 `/var/www/first_book_v2/backend/` 目录下创建 `.env` 文件：

```env
# 你的 Gmail 地址
EMAIL_USER=your-email@gmail.com

# 你的 Gmail 应用专用密码 (16位)
EMAIL_PASS=your-16-character-app-password

# 服务器端口
PORT=3001
```

### 3. 重启后端服务器

```bash
cd /var/www/first_book_v2/backend
node server.js
```

## 🧪 测试功能

### 测试邮箱
使用已注册的邮箱地址：
- `emma@example.com`
- `testuser2@example.com`
- `newemail@example.com`

### API 端点
- **发送验证码**: `POST /api/auth/send-verification-code`
- **重置密码**: `POST /api/auth/reset-password`

## ⚠️ 注意事项

1. **必须使用应用专用密码**，不能使用 Gmail 登录密码
2. **验证码有效期**: 5 分钟
3. **测试模式**: 响应中会包含验证码用于测试
4. **生产环境**: 移除 `testCode` 字段

## 🔒 安全建议

- 不要将 `.env` 文件提交到 Git
- 使用环境变量管理敏感信息
- 考虑使用专业的邮件服务 (SendGrid, Mailgun)
